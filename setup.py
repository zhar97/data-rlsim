"""
Package `data-rlsim` installation based on `pyproject.toml` |
`setup.cfg`. Executable may be called as `data-rlsim` but it is
`data_rlsim` through python import.      
"""

from setuptools import setup

setup()